[package]
name = "veilnet"
version = "0.1.0"
edition = "2021"

[dependencies]
oqs = { version = "0.9", default-features = false, features = ["kems", "sigs"] }
tokio = { version = "1", features = ["full"] }
